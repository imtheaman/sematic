import { NextPage } from "next";

const CurrentProjectInsights: NextPage = () => {
  return (
    <div className="bg-white w-full h-48 shadow rounded-lg flex items-center justify-center">
      insights will be available after connecting to the database
    </div>
  );
};

export default CurrentProjectInsights;
